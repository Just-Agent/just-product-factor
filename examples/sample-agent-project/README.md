# Sample Agent Project

A tiny placeholder project used by `run_skill_audit.py` smoke tests.

It intentionally contains minimal files so the audit runner can detect project signals and create an audit package.
